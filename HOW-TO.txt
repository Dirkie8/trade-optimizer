# Run optimization
#-----------------
#-----------------

# NEW: Bayesian optimization (Recommended!)
#------------------------------------------
# Uses Optuna TPE sampler with custom reward metric
# Automatically holds out 20% for validation
# See BAYESIAN_OPTIMIZATION.md for full documentation

# Single strategy:
python scripts/optimize_bayesian.py \
  --strategy_config functions/configs/rsi_strategy.yaml \
  --main_config configs/main_config.yaml \
  --n_trials 100 \
  --n_jobs 4

# With more trials for complex strategies:
python scripts/optimize_bayesian.py \
  --strategy_config functions/configs/multi_indicator_confluence.yaml \
  --main_config configs/main_config.yaml \
  --n_trials 300 \
  --n_jobs 8

# BATCH: Optimize all strategies at once
./optimize_bayesian_batch.sh --n_trials 100 --n_jobs 4

# Quick batch test (50 trials, single core)
./optimize_bayesian_batch.sh --n_trials 50 --n_jobs 1

# Full batch optimization (500 trials, 8 cores)
./optimize_bayesian_batch.sh --n_trials 500 --n_jobs 8

OR

# Normal run:
python scripts/optimize_strategy.py \         
--strategy_config functions/configs/atr_channel_breakout.yaml \
--main_config configs/main_config.yaml \
--method grid \
--n_random 200 \
--n_jobs 4

python scripts/evaluate_strategy.py \       
    --strategy_config functions/configs/stochastic_oscillator.yaml \
    --main_config configs/main_config.yaml \
    --optimization_csv results/evaluations/evaluation_results.csv

&&&&&&&& Batch
./optimize_all_strategies.sh --method random --n_random 500 --n_jobs 5

OR

# Run with walk-forward to reduce overfitting
#--------------------------------------------
python scripts/optimize_strategy.py \
  --strategy_config functions/configs/rsi_strategy.yaml \
  --main_config configs/main_config.yaml \
  --method random \
  --n_random 50 \
  --walk-forward \
  --n-folds 5 \
  --sort-by consistency_score

&&&&&&&& Batch
./optimize_with_walk_forward.sh

OR

# Combine random exploration with tpe optimizer
#-----------------------
python scripts/hybrid_optimize.py \
  --strategy_config functions/configs/rsi_strategy.yaml \
  --main_config configs/main_config.yaml \
  --random_samples 30 \
  --tpe_trials 50 \
  --walk-forward

OR

# Combine gradient optimization + overfitting prevention:
#--------------------------------------------------------
python scripts/optimize_strategy.py \
  --strategy_config functions/configs/rsi_strategy.yaml \
  --main_config configs/main_config.yaml \
  --method tpe \
  --n_trials 50 \
  --walk-forward \
  --n-folds 3


# Best approach: random + tpe + walk-forward
#-------------------------------------------
python scripts/hybrid_optimize.py \
  --strategy_config functions/configs/rsi_strategy.yaml \
  --main_config configs/main_config.yaml \
  --random_samples 200 \
  --tpe_trials 50 \
  --walk-forward \
  --n-folds 4 \
  --n_jobs 1

&&&&&& Batch with logs
./optimize_hybrid_batch.sh \
--random_samples 500 \
--tpe_trials 50 \
--walk-forward \
--n-folds 4 \
--n_jobs 4

mkdir -p logs
ts=$(date "+%Y-%m-%d_%H-%M-%S")
./optimize_hybrid_batch.sh \
  --random_samples 200 \
  --tpe_trials 50 \
  --walk-forward \
  --n-folds 4 \
  --n_jobs 5 \
  2>&1 | tee "logs/optim_${ts}.log"

./optimize_hybrid_batch.sh --help

15 minute run
./optimize_hybrid_batch.sh \
  --random_samples 10 \
  --tpe_trials 10 \
  --walk-forward \
  --n-folds 3 \
  --n_jobs 5



# How walk through works:
#%%%---------------------
########Without wt:
For each of 50 random parameter combinations:
    Run backtest on 80% of data (training)
    → ONE backtest per parameter set
    
Total: 50 backtests

Then take top 50 and evaluate on 20% (test set)
Total: 50 + 50 = 100 backtests

######## With wt:
For each of 50 random parameter combinations:
    Split data into 5 folds
    
    Fold 1: Train on [0-20%] → Test on [20-40%]  ← 1 backtest
    Fold 2: Train on [0-40%] → Test on [40-60%]  ← 1 backtest
    Fold 3: Train on [0-60%] → Test on [60-80%]  ← 1 backtest
    Fold 4: Train on [0-80%] → Test on [80-100%] ← 1 backtest
    
    Average results from 4 test folds
    → FOUR backtests per parameter set (actually 4, not 5, because fold 1 has no training data)

Total: 50 × 4 = 200 backtests during optimization

Then still evaluate top 50 on hold-out test set
Total: 200 + 50 = 250 backtests





# Run best parameters over full dataset/ evaluate best params
#------------------------------------------------------------
scripts/evaluate_strategy.py \
  --strategy_config functions/configs/adx_trend.yaml \
  --main_config configs/main_config.yaml

# Run backtest on 100% of data with best StochasticOscillator parameters
./run_full_backtest.sh StochasticOscillator
OR all
./run_full_backtest.sh --all



# Run plotting scripts:
#----------------------
# Create plot
python scripts/plot_results.py --input results/RSIStrategy/full_dataset_backtest.json
# Create plot and show
python scripts/plot_results.py --input results/RSIStrategy/full_dataset_backtest.json --show
# Create plot and specify output folder
python scripts/plot_results.py --input results/RSIStrategy/full_dataset_backtest.json --output my_custom_plot.png

# Batch plot into evaluate folders
python scripts/plot_results.py --batch
# Batch plot into output folder
python scripts/plot_results.py --batch --output-root results/images




# Compare all strategies following optimisation
#----------------------------------------------
./compare_all_strategy_performance_metrics.sh OR
python scripts/compare_strategies.py

# Sort by return percentage
./compare_all_strategy_performance_metrics.sh --sort return
python scripts/compare_strategies.py --sort return


# Sort by drawdown (lowest first)
./compare_all_strategy_performance_metrics.sh --sort drawdown

# Sort by number of trades
./compare_all_strategy_performance_metrics.sh --sort trades

# Sort by win rate
./compare_all_strategy_performance_metrics.sh --sort winrate

# Only show strategies with at least 50 trades
./compare_all_strategy_performance_metrics.sh --min-trades 50

# Only show strategies with at least 100 trades, sorted by return
./compare_all_strategy_performance_metrics.sh --min-trades 100 --sort return















I want you to come up with 5 trading strategies and create the classes and configs for them. This can be anything from complex, utilising any indicator to very simple. Go wild and do your best.

Now typically I would run something like this:

python scripts/optimize_strategy.py \         
    --strategy_config functions/configs/example_strategy.yaml \
    --main_config configs/main_config.yaml \
    --method grid \
    --n_random 20 \
    --n_jobs 4

to optimize a strategy, however, I want you to also create me a .sh script that will execute optimisations for each of the strategies in the 


Execution realism and risk controls (optional)
----------------------------------------------
You can enable a few optional controls in `configs/main_config.yaml` under the `account` block
to make backtests more realistic and to prevent the account from “drying up”. All are optional
and default to off if omitted.

Keys:
- max_drawdown_stop_pct: Stop trading once peak-to-trough drawdown exceeds this percent.
- min_stop_pips: Skip trades whose stop-loss distance is tighter than this (in pips).
- slippage_pips: Apply additional unfavorable slippage at entry and exit (in pips) on top of spread.
- min_size: Skip trades whose calculated size is below this threshold (prevents micro-trades).
- leverage: Cap position size by margin: notional <= equity * leverage.
- max_concurrent_positions: NEW. Maximum number of simultaneous open trades allowed per backtest/optimization run. Default 1 (legacy single-position behavior). Set >1 to allow layering multiple positions as signals arrive.

Example account block with conservative settings:

  account:
    starting_balance: 100
    risk_per_trade: 0.01
    spread_pips: 0.2
    commission_per_trade: 0.0
    leverage: 100
    max_drawdown_stop_pct: 30     # stop trading after 30% drawdown
    min_stop_pips: 3              # ignore SLs tighter than 3 pips
    slippage_pips: 0.1            # add 0.1 pips slippage per fill
    min_size: 0.0001              # ignore trades smaller than this size
    max_concurrent_positions: 3   # allow up to 3 trades open at once during backtests/optimization

Notes:
- Spread is modeled as half at entry and half at exit (unfavorable), in addition to slippage if set.
- These settings are optional. If omitted or set to 0, default behavior is unchanged.
- The walk-forward optimizer’s validation split is controlled by its CLI flag `--validation_ratio` (batch runner passes it through).
- Concurrency: If `max_concurrent_positions` > 1 the backtester attempts new entries while other trades are still open, up to the cap. Strategies designed for single-trade logic will still work; they may simply add multiple similar positions over consecutive bars.



##### 2025-11-07 Latest flow
##### ---------

./optimize_bayesian_wf_batch.sh \
--n_trials 800 \
--n_jobs 4 \
--n_folds 8 \
--validation_ratio 0.20 \
--reward balanced \
--param_stagnation_patience 25 \
--param_tolerance 0.0001 \
--yes \
--include "KeltnerSqueezeBreakout,KeltnerSqueezeBreakoutAutoSl"

--include "DICross,FractalBreakout,HeikinAshiTrend,KeltnerSqueezeBreakout,KSTCrossover,PivotReversal,SMASlopeBreakout,StochasticOscillator"
--include "AroonTrend,ATRBandReversion,BollingerBreakout,BollingerSqueezeBreakout,DICross,EMACrossover,EMAPullback,FractalBreakout,HeikinAshiReversal,HeikinAshiTrend,HullMACrossover,IchimokuKijunCross,KeltnerSqueezeBreakout,KSTCrossover,MACDMomentum,PivotReversal,PPOCrossover,PriceChannelReversion,SMASlopeBreakout,StochasticOscillator,StochRSIStrategy" \

# Evaluation
./run_full_backtest.sh --all
run_full_backtest.sh --all --param-source best_yaml
python scripts/evaluate_strategy.py \
  --strategy_config functions/configs/keltner_squeeze_breakout.yaml \
  --main_config configs/main_config.yaml

# Plotting and comparison
python scripts/plot_results.py --batch --output-root results/images
python scripts/compare_bayesian_results.py --output results/analysis_opt.md
python scripts/compare_bayesian_results.py --subdir evaluations --output results/analysis_evals.md

python scripts/plot_results.py --input results/replay/replay_keltner.json --output-root results/replay
python scripts/plot_results.py --input results/keltner_squeeze_breakout/evaluations/full_dataset_backtest.json --output-root images3


DICross,FractalBreakout,HeikinAshiTrend,KeltnerSqueezeBreakout,KSTCrossover,PivotReversal,SMASlopeBreakout,StochasticOscillator" \

# Trading bot
python scripts/trading_bot.py --config configs/trading_bot_config.yaml --once --dry-run
python scripts/trading_bot.py --config configs/trading_bot_config.yaml \
  --test-trade --test-dry-run --test-auto-adjust

# Test trading bot on data
python scripts/trading_bot.py --mode replay --config configs/trading_bot_config.yaml --replay-output results/replay/replay_keltner.json --replay-start 2025-11-17 --replay-end 2025-11-22
Run a short replay and save results:
  python scripts/trading_bot.py --mode replay \
    --config configs/trading_bot_config.yaml \
    --replay-output results_test/replay_keltner.json \
    --replay-start 2024-10-01 --replay-end 2024-10-04
  python scripts/trading_bot.py --mode replay \
    --config configs/trading_bot_config.yaml \
    --replay-output results/replay/keltner.json \
    --replay-start 2025-01-01 --replay-end 2025-03-01
To switch strategy/timeframe/symbol:
code configs/trading_bot_config.yaml  # edit the 'strategies' section (strategy_config, symbol, timeframe)

# Test to see if the optimizer and backtester "does the same thing"
python check_eval_vs_backtest.py --eval full_dataset_backtest.json --strategy_config keltner_squeeze_breakout.yaml






##### 2025-12-17 Latest flow
##### ---------

============================================================
Quick explainer (v2 alignment) and how to run
============================================================

Why v2?
- Train, evaluate, and trade all call the same strategy code path so live runs match what was optimized.
- Uses a robust equity-based reward (not Sharpe) that favors smooth, drawdown-aware growth.
- Keeps a strict hold-out validation set that’s only evaluated once after optimization (no leakage).

What’s new in v2
- Optimizer: scripts/optimize_bayesian_walkforward_v2.py
  - Bayesian (Optuna TPE) + walk-forward on training only.
  - Objective: reward_with_constraints(equity) with hard DD/terminal constraints.
  - Fold aggregation configurable (mean, median, mean - λ·std) in configs/main_config.v2.yaml.
  - Artifacts: results/<strategy_snake>/optimizations/
    - bayesian_wf_v2_results.csv (all trials)
    - bayesian_wf_v2_best.json (best params + metadata)

- Evaluator: scripts/evaluate_strategy_v2.py
  - Uses the same backtest path as optimizer (alignment).
  - Picks params from v2 best artifact by default, with legacy fallbacks or YAML.
  - Output: results/<strategy_snake>/evaluations/full_backtest_v2.json

- Trading bot: scripts/trading_bot_v2.py
  - Candle-close execution; SL/TP determined by the strategy (no implicit pips scaling).
  - Lot sizing and broker margin checks retained; concise logs and CSV journals.
  - Params source per strategy: YAML best_params or results best JSON (auto-discovered or explicit path).

Configs (v2)
- configs/main_config.v2.yaml: symbol/timeframe, account, optimization fold settings, reward params (k, eps, max DD).
- configs/trading_bot_config.v2.yaml: account creds/runtime, risk, strategies list and params source.

Reward function intuition
- equity_reward multiplies scale-free growth (log-return) by smoothness terms:
  - (1 − underwater_ratio) and exp(−k · normalized drawdown area),
  - driving the search toward steady, low-drawdown equity curves.
- reward_with_constraints rejects parameter sets with excessive DD or no terminal growth.

How to run (v2)
1) Prepare environment
   - Ensure your Python env has: optuna, numpy, pandas, pyyaml, tqdm.
   - For trading bot: MetaTrader5 and python-dotenv (if using .env for creds).

2) Configure v2
   - Edit configs/main_config.v2.yaml (symbol, timeframe, account, optimization, reward).
   - Ensure your strategy YAML (functions/configs/<strategy>.yaml) includes:
     - strategy.module and strategy.class
     - parameters_bayesian ranges for optimization
     - optional best_params for deployment

3) Optimize (train-only, walk-forward)
   python scripts/optimize_bayesian_walkforward_v2.py \
     --strategy KeltnerSqueezeBreakout \
     --n_trials 100 \
     --n_jobs 1

   Outputs under results/<strategy_snake>/optimizations/:
   - bayesian_wf_v2_results.csv
   - bayesian_wf_v2_best.json (used by evaluator/bot if selecting from results)

4) Evaluate on full data
   python scripts/evaluate_strategy_v2.py \
     --strategy KeltnerSqueezeBreakout \
     --params_source auto

   Output: results/<strategy_snake>/evaluations/full_backtest_v2.json

5) (Optional) Plot results
   python scripts/plot_results.py --input results/<strategy_snake>/evaluations/full_backtest_v2.json
   python scripts/plot_results.py --input results/keltner_squeeze_breakout/evaluations/full_backtest_v2.json
   python scripts/plot_results.py --input results/keltner_squeeze_breakout/evaluations/validation_backtest_v2.json

6) Trade live or paper (MT5)
   - Edit configs/trading_bot_config.v2.yaml:
     - Set account creds (or use .env: MT5_LOGIN, MT5_PASSWORD, MT5_SERVER)
     - For each enabled strategy, set params_source:
       - yaml (use best_params in YAML), or
       - results (auto-discover bayesian_wf_v2_best.json or set params_results_path)

   Run:
   python scripts/trading_bot_v2.py --config configs/trading_bot_config.v2.yaml

   Useful flags:
   - --dry-run (compute and log orders only)
   - --skip-start (wait for next candle close before first evaluation)
   - --once (process one cycle and exit)

Notes
- v1 scripts remain for reference; prefer v2 for new runs.
- Validation set is never used during optimization; only scored once at the end.
- If you want more conservative selection, set fold aggregation to mean_var_penalty and tune variance_penalty_lambda.











python scripts/trading_bot.py --mode replay \
  --config configs/trading_bot_config.yaml \
  --replay-output results/replay/replay_keltner.json \
  --replay-start 2025-11-17 --replay-end 2025-11-22 \
  --replay-model parity_bot
  
python scripts/evaluate_strategy.py \
  --strategy_config functions/configs/keltner_squeeze_breakout.yaml \
  --main_config configs/main_config.yaml

python scripts/trading_bot.py --mode replay \
  --config configs/trading_bot_config.yaml \
  --replay-output results/replay/replay_keltner.json \
  --replay-start 2025-11-24 --replay-end 2025-12-06

python scripts/plot_results.py --input results/keltner_squeeze_breakout/evaluations/full_dataset_backtest.json --output-root images3
python scripts/plot_results.py --input results/replay/replay_keltner.json --output-root results/replay

